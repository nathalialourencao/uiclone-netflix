/* eslint-disable import/no-anonymous-default-export */
import React from "react";

import { Footer } from './styles';

export default () => {
    return (
        <Footer>
            Feito com <span role="img" aria-label="coração">❤️</span> por Nathália Lourenção.
        </Footer>
    );
};